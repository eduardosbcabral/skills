interface:
  display_name: "Create Wiki Notes"
  short_description: "Create simple Eduardo Wiki notes"
  default_prompt: "Use $create-wiki-notes to create a concise note in Eduardo Wiki."
policy:
  allow_implicit_invocation: true
