interface:
  display_name: "Loop State And Stall Guard"
  short_description: "Persist software loop state and detect stalls"
  default_prompt: "Use $loop-state-and-stall-guard to persist this active software-development loop and avoid repeating stalled technical attempts."
