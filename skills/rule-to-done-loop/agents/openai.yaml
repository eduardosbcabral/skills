interface:
  display_name: "Rule to Done Loop"
  short_description: "Implement a rule with focused verification"
  default_prompt: "Use $rule-to-done-loop to turn this business rule into an implemented, verified change."
